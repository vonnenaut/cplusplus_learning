#include <cstdio>

int main() {
    int x = 2;
    int y = 7;

    printf("value is %d\n", x > y);

    if(x > y) {
        puts("condition is true");
    } else {
        puts("condition is false");
    }

    return 0;
}
